/**
 *
 *  MetadataProviders.cc
 *  DO NOT EDIT. This file is generated by drogon_ctl
 *
 */

#include "MetadataProviders.h"
#include <drogon/utils/Utilities.h>
#include <string>

using namespace drogon;
using namespace drogon::orm;
using namespace drogon_model::sqlite3;

const std::string MetadataProviders::Cols::_id = "id";
const std::string MetadataProviders::Cols::_provider = "provider";
const std::string MetadataProviders::primaryKeyName = "id";
const bool MetadataProviders::hasPrimaryKey = true;
const std::string MetadataProviders::tableName = "metadata_providers";

const std::vector<typename MetadataProviders::MetaData> MetadataProviders::metaData_={
{"id","int64_t","integer",8,1,1,1},
{"provider","std::string","text",0,0,0,1}
};
const std::string &MetadataProviders::getColumnName(size_t index) noexcept(false)
{
    assert(index < metaData_.size());
    return metaData_[index].colName_;
}
MetadataProviders::MetadataProviders(const Row &r, const ssize_t indexOffset) noexcept
{
    if(indexOffset < 0)
    {
        if(!r["id"].isNull())
        {
            id_=std::make_shared<int64_t>(r["id"].as<int64_t>());
        }
        if(!r["provider"].isNull())
        {
            provider_=std::make_shared<std::string>(r["provider"].as<std::string>());
        }
    }
    else
    {
        size_t offset = (size_t)indexOffset;
        if(offset + 2 > r.size())
        {
            LOG_FATAL << "Invalid SQL result for this model";
            return;
        }
        size_t index;
        index = offset + 0;
        if(!r[index].isNull())
        {
            id_=std::make_shared<int64_t>(r[index].as<int64_t>());
        }
        index = offset + 1;
        if(!r[index].isNull())
        {
            provider_=std::make_shared<std::string>(r[index].as<std::string>());
        }
    }

}

MetadataProviders::MetadataProviders(const Json::Value &pJson, const std::vector<std::string> &pMasqueradingVector) noexcept(false)
{
    if(pMasqueradingVector.size() != 2)
    {
        LOG_ERROR << "Bad masquerading vector";
        return;
    }
    if(!pMasqueradingVector[0].empty() && pJson.isMember(pMasqueradingVector[0]))
    {
        dirtyFlag_[0] = true;
        if(!pJson[pMasqueradingVector[0]].isNull())
        {
            id_=std::make_shared<int64_t>((int64_t)pJson[pMasqueradingVector[0]].asInt64());
        }
    }
    if(!pMasqueradingVector[1].empty() && pJson.isMember(pMasqueradingVector[1]))
    {
        dirtyFlag_[1] = true;
        if(!pJson[pMasqueradingVector[1]].isNull())
        {
            provider_=std::make_shared<std::string>(pJson[pMasqueradingVector[1]].asString());
        }
    }
}

MetadataProviders::MetadataProviders(const Json::Value &pJson) noexcept(false)
{
    if(pJson.isMember("id"))
    {
        dirtyFlag_[0]=true;
        if(!pJson["id"].isNull())
        {
            id_=std::make_shared<int64_t>((int64_t)pJson["id"].asInt64());
        }
    }
    if(pJson.isMember("provider"))
    {
        dirtyFlag_[1]=true;
        if(!pJson["provider"].isNull())
        {
            provider_=std::make_shared<std::string>(pJson["provider"].asString());
        }
    }
}

void MetadataProviders::updateByMasqueradedJson(const Json::Value &pJson,
                                            const std::vector<std::string> &pMasqueradingVector) noexcept(false)
{
    if(pMasqueradingVector.size() != 2)
    {
        LOG_ERROR << "Bad masquerading vector";
        return;
    }
    if(!pMasqueradingVector[0].empty() && pJson.isMember(pMasqueradingVector[0]))
    {
        if(!pJson[pMasqueradingVector[0]].isNull())
        {
            id_=std::make_shared<int64_t>((int64_t)pJson[pMasqueradingVector[0]].asInt64());
        }
    }
    if(!pMasqueradingVector[1].empty() && pJson.isMember(pMasqueradingVector[1]))
    {
        dirtyFlag_[1] = true;
        if(!pJson[pMasqueradingVector[1]].isNull())
        {
            provider_=std::make_shared<std::string>(pJson[pMasqueradingVector[1]].asString());
        }
    }
}

void MetadataProviders::updateByJson(const Json::Value &pJson) noexcept(false)
{
    if(pJson.isMember("id"))
    {
        if(!pJson["id"].isNull())
        {
            id_=std::make_shared<int64_t>((int64_t)pJson["id"].asInt64());
        }
    }
    if(pJson.isMember("provider"))
    {
        dirtyFlag_[1] = true;
        if(!pJson["provider"].isNull())
        {
            provider_=std::make_shared<std::string>(pJson["provider"].asString());
        }
    }
}

const int64_t &MetadataProviders::getValueOfId() const noexcept
{
    static const int64_t defaultValue = int64_t();
    if(id_)
        return *id_;
    return defaultValue;
}
const std::shared_ptr<int64_t> &MetadataProviders::getId() const noexcept
{
    return id_;
}
void MetadataProviders::setId(const int64_t &pId) noexcept
{
    id_ = std::make_shared<int64_t>(pId);
    dirtyFlag_[0] = true;
}
const typename MetadataProviders::PrimaryKeyType & MetadataProviders::getPrimaryKey() const
{
    assert(id_);
    return *id_;
}

const std::string &MetadataProviders::getValueOfProvider() const noexcept
{
    static const std::string defaultValue = std::string();
    if(provider_)
        return *provider_;
    return defaultValue;
}
const std::shared_ptr<std::string> &MetadataProviders::getProvider() const noexcept
{
    return provider_;
}
void MetadataProviders::setProvider(const std::string &pProvider) noexcept
{
    provider_ = std::make_shared<std::string>(pProvider);
    dirtyFlag_[1] = true;
}
void MetadataProviders::setProvider(std::string &&pProvider) noexcept
{
    provider_ = std::make_shared<std::string>(std::move(pProvider));
    dirtyFlag_[1] = true;
}

void MetadataProviders::updateId(const uint64_t id)
{
    id_ = std::make_shared<int64_t>(static_cast<int64_t>(id));
}

const std::vector<std::string> &MetadataProviders::insertColumns() noexcept
{
    static const std::vector<std::string> inCols={
        "provider"
    };
    return inCols;
}

void MetadataProviders::outputArgs(drogon::orm::internal::SqlBinder &binder) const
{
    if(dirtyFlag_[1])
    {
        if(getProvider())
        {
            binder << getValueOfProvider();
        }
        else
        {
            binder << nullptr;
        }
    }
}

const std::vector<std::string> MetadataProviders::updateColumns() const
{
    std::vector<std::string> ret;
    if(dirtyFlag_[1])
    {
        ret.push_back(getColumnName(1));
    }
    return ret;
}

void MetadataProviders::updateArgs(drogon::orm::internal::SqlBinder &binder) const
{
    if(dirtyFlag_[1])
    {
        if(getProvider())
        {
            binder << getValueOfProvider();
        }
        else
        {
            binder << nullptr;
        }
    }
}
Json::Value MetadataProviders::toJson() const
{
    Json::Value ret;
    if(getId())
    {
        ret["id"]=(Json::Int64)getValueOfId();
    }
    else
    {
        ret["id"]=Json::Value();
    }
    if(getProvider())
    {
        ret["provider"]=getValueOfProvider();
    }
    else
    {
        ret["provider"]=Json::Value();
    }
    return ret;
}

std::string MetadataProviders::toString() const
{
    return toJson().toStyledString();
}

Json::Value MetadataProviders::toMasqueradedJson(
    const std::vector<std::string> &pMasqueradingVector) const
{
    Json::Value ret;
    if(pMasqueradingVector.size() == 2)
    {
        if(!pMasqueradingVector[0].empty())
        {
            if(getId())
            {
                ret[pMasqueradingVector[0]]=(Json::Int64)getValueOfId();
            }
            else
            {
                ret[pMasqueradingVector[0]]=Json::Value();
            }
        }
        if(!pMasqueradingVector[1].empty())
        {
            if(getProvider())
            {
                ret[pMasqueradingVector[1]]=getValueOfProvider();
            }
            else
            {
                ret[pMasqueradingVector[1]]=Json::Value();
            }
        }
        return ret;
    }
    LOG_ERROR << "Masquerade failed";
    if(getId())
    {
        ret["id"]=(Json::Int64)getValueOfId();
    }
    else
    {
        ret["id"]=Json::Value();
    }
    if(getProvider())
    {
        ret["provider"]=getValueOfProvider();
    }
    else
    {
        ret["provider"]=Json::Value();
    }
    return ret;
}

bool MetadataProviders::validateJsonForCreation(const Json::Value &pJson, std::string &err)
{
    if(pJson.isMember("id"))
    {
        if(!validJsonOfField(0, "id", pJson["id"], err, true))
            return false;
    }
    if(pJson.isMember("provider"))
    {
        if(!validJsonOfField(1, "provider", pJson["provider"], err, true))
            return false;
    }
    else
    {
        err="The provider column cannot be null";
        return false;
    }
    return true;
}
bool MetadataProviders::validateMasqueradedJsonForCreation(const Json::Value &pJson,
                                                           const std::vector<std::string> &pMasqueradingVector,
                                                           std::string &err)
{
    if(pMasqueradingVector.size() != 2)
    {
        err = "Bad masquerading vector";
        return false;
    }
    try {
      if(!pMasqueradingVector[0].empty())
      {
          if(pJson.isMember(pMasqueradingVector[0]))
          {
              if(!validJsonOfField(0, pMasqueradingVector[0], pJson[pMasqueradingVector[0]], err, true))
                  return false;
          }
      }
      if(!pMasqueradingVector[1].empty())
      {
          if(pJson.isMember(pMasqueradingVector[1]))
          {
              if(!validJsonOfField(1, pMasqueradingVector[1], pJson[pMasqueradingVector[1]], err, true))
                  return false;
          }
        else
        {
            err="The " + pMasqueradingVector[1] + " column cannot be null";
            return false;
        }
      }
    }
    catch(const Json::LogicError &e)
    {
      err = e.what();
      return false;
    }
    return true;
}
bool MetadataProviders::validateJsonForUpdate(const Json::Value &pJson, std::string &err)
{
    if(pJson.isMember("id"))
    {
        if(!validJsonOfField(0, "id", pJson["id"], err, false))
            return false;
    }
    else
    {
        err = "The value of primary key must be set in the json object for update";
        return false;
    }
    if(pJson.isMember("provider"))
    {
        if(!validJsonOfField(1, "provider", pJson["provider"], err, false))
            return false;
    }
    return true;
}
bool MetadataProviders::validateMasqueradedJsonForUpdate(const Json::Value &pJson,
                                                         const std::vector<std::string> &pMasqueradingVector,
                                                         std::string &err)
{
    if(pMasqueradingVector.size() != 2)
    {
        err = "Bad masquerading vector";
        return false;
    }
    try {
      if(!pMasqueradingVector[0].empty() && pJson.isMember(pMasqueradingVector[0]))
      {
          if(!validJsonOfField(0, pMasqueradingVector[0], pJson[pMasqueradingVector[0]], err, false))
              return false;
      }
    else
    {
        err = "The value of primary key must be set in the json object for update";
        return false;
    }
      if(!pMasqueradingVector[1].empty() && pJson.isMember(pMasqueradingVector[1]))
      {
          if(!validJsonOfField(1, pMasqueradingVector[1], pJson[pMasqueradingVector[1]], err, false))
              return false;
      }
    }
    catch(const Json::LogicError &e)
    {
      err = e.what();
      return false;
    }
    return true;
}
bool MetadataProviders::validJsonOfField(size_t index,
                                         const std::string &fieldName,
                                         const Json::Value &pJson,
                                         std::string &err,
                                         bool isForCreation)
{
    switch(index)
    {
        case 0:
            if(pJson.isNull())
            {
                err="The " + fieldName + " column cannot be null";
                return false;
            }
            if(isForCreation)
            {
                err="The automatic primary key cannot be set";
                return false;
            }
            if(!pJson.isInt64())
            {
                err="Type error in the "+fieldName+" field";
                return false;
            }
            break;
        case 1:
            if(pJson.isNull())
            {
                err="The " + fieldName + " column cannot be null";
                return false;
            }
            if(!pJson.isString())
            {
                err="Type error in the "+fieldName+" field";
                return false;
            }
            break;
        default:
            err="Internal error in the server";
            return false;
    }
    return true;
}
