#include "api_Items.h"

using namespace api;
void Items::getItems(const HttpRequestPtr &req,
               std::function<void (const HttpResponsePtr &)> &&callback)
{}
// Add definition of your processing function here
